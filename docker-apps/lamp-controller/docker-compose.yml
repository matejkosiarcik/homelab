x-definitions:
  common-env-variables: &common-env-variables
    HOMELAB_APP_NAME: lamp-controller
  common-service-options: &common-service-options
    environment:
      <<: *common-env-variables
    # env_file:
    #   - ./config/common.env
    networks:
      internal-network: {}

services:
  app-hardware-controller:
    <<: *common-service-options
    build:
      context: ../../../../docker-images/
      dockerfile: ./custom/lamp-hardware-controller/Dockerfile
    volumes:
      - ./app-data/status:/app/status:rw
      - ./app-logs/hardware-controller:/log:rw

  app-network-server:
    <<: *common-service-options
    build:
      context: ../../../../docker-images/
      dockerfile: ./custom/lamp-network-server/Dockerfile
    volumes:
      - ./app-data/status:/app/status:rw
      - ./app-logs/network-server:/log:rw

  certificate-manager:
    <<: *common-service-options
    build:
      context: ../../../../docker-images/
      dockerfile: ./custom/certificate-manager/Dockerfile
    env_file:
      - ./app-secrets/certificate-manager.env
    volumes:
      - ./app-data/certs:/certs:rw
      - ./app-logs/certificate-manager:/log:rw

  http-proxy:
    <<: *common-service-options
    build:
      context: ../../../../docker-images/
      dockerfile: ./custom/http-proxy/Dockerfile
    depends_on:
      certificate-manager:
        condition: service_healthy
    volumes:
      - ./app-logs/http-proxy:/log:rw
      - ./app-data/certs:/certs:ro
      - ./app-secrets/http-user--proxy-status.htpasswd:/app/misc/http-user--proxy-status.htpasswd:ro

  socket-proxy:
    <<: *common-service-options
    build:
      context: ../../../../docker-images/
      dockerfile: ./custom/socket-proxy/Dockerfile
    volumes:
      - ./app-logs/socket-proxy:/log:rw

networks:
  internal-network: {}
