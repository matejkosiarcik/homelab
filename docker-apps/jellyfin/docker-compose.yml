x-definitions:
  common-env-variables: &common-env-variables
    HOMELAB_APP_NAME: jellyfin
  common-service-options: &common-service-options
    environment:
      <<: *common-env-variables
    # env_file:
    #   - ./config/common.env
    networks:
      internal-network: {}

services:
  jellyfin:
    <<: *common-service-options
    hostname: ${DOCKER_COMPOSE_DOMAIN_PREFIX} # This fixes random container hostname in settings and server picker (everything after dot is ignored, so it cannot be full domain)
    build:
      context: ../../../../docker-images/
      dockerfile: ./external/jellyfin/Dockerfile
    volumes:
      - ./app-data/jellyfin-data:/config

  certificate-manager:
    <<: *common-service-options
    build:
      context: ../../../../docker-images/
      dockerfile: ./custom/certificate-manager/Dockerfile
    env_file:
      - ./app-secrets/certificate-manager.env
    volumes:
      - ./app-data/certs:/homelab/certs:rw
      - ./app-logs/certificate-manager:/homelab/logs:rw

  http-proxy:
    <<: *common-service-options
    build:
      context: ../../../../docker-images/
      dockerfile: ./custom/http-proxy/Dockerfile
    depends_on:
      certificate-manager:
        condition: service_healthy
    volumes:
      - ./app-logs/http-proxy:/homelab/logs:rw
      - ./app-data/certs:/homelab/certs:ro
      - ./app-secrets/http-user--proxy-status.htpasswd:/homelab/http-user--proxy-status.htpasswd:ro

  socket-proxy:
    <<: *common-service-options
    build:
      context: ../../../../docker-images/
      dockerfile: ./custom/socket-proxy/Dockerfile

networks:
  internal-network: {}
