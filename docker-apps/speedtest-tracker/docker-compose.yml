x-definitions:
  common-env-variables: &common-env-variables
    HOMELAB_APP_NAME: speedtest-tracker
  common-service-options: &common-service-options
    environment:
      <<: *common-env-variables
    # env_file:
    #   - ./config/common.env
    networks:
      internal-network: {}

services:
  main-app:
    <<: *common-service-options
    build:
      context: ../../../../docker-images/
      dockerfile: ./external/speedtest-tracker/Dockerfile
    environment:
      <<: *common-env-variables
    env_file:
      - ./private/app.env
    volumes:
      - ./data/app-data:/config
      - ./log/main-app:/log:rw

  certificate-manager:
    <<: *common-service-options
    build:
      context: ../../../../docker-images/
      dockerfile: ./custom/certificate-manager/Dockerfile
    env_file:
      - ./private/certificate-manager.env
    volumes:
      - ./data/certs:/certs:rw
      - ./log/certificate-manager:/log:rw

  http-proxy:
    <<: *common-service-options
    build:
      context: ../../../../docker-images/
      dockerfile: ./custom/http-proxy/Dockerfile
    depends_on:
      certificate-manager:
        condition: service_healthy
    volumes:
      - ./data/certs:/certs:ro
      - ./private/http-user--proxy-status.htpasswd:/app/misc/http-user--proxy-status.htpasswd:ro
      - ./log/http-proxy:/log:rw

  socket-proxy:
    <<: *common-service-options
    build:
      context: ../../../../docker-images/
      dockerfile: ./custom/socket-proxy/Dockerfile
    volumes:
      - ./log/socket-proxy:/log:rw

  web-admin-setup:
    <<: *common-service-options
    build:
      context: ../../../../docker-images/
      dockerfile: ./custom/web-automation/Dockerfile
    depends_on:
      main-app:
        condition: service_healthy
    env_file:
      - ./private/web-admin-setup.env
    environment:
      <<: *common-env-variables
      HOMELAB_CONTAINER_VARIANT: admin-setup
    volumes:
      - ./log/web-admin-setup-errors:/errors:rw
      - ./log/web-admin-setup:/log:rw

  # web-export:
  #   <<: *common-service-options
  #   build:
  #     context: ../../../../docker-images/
  #     dockerfile: ./custom/web-automation/Dockerfile
  #   depends_on:
  #     main-app:
  #       condition: service_healthy
  #     web-admin-setup:
  #       condition: service_healthy
  #   env_file:
  #     - ./private/web-export.env
  #   environment:
  #     <<: *common-env-variables
  #     HOMELAB_CONTAINER_VARIANT: export
  #   volumes:
  #     - ./data/web-export:/export:rw
  #     - ./log/web-export-errors:/errors:rw
  #     - ./log/web-export:/log:rw

networks:
  internal-network: {}
