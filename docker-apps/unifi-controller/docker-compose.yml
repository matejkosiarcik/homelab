x-definitions:
  common-env-variables: &common-env-variables
    HOMELAB_APP_NAME: unifi-controller
  common-service-options: &common-service-options
    environment:
      <<: *common-env-variables
    networks:
      internal-network: {}

services:
  unifi-network-app:
    <<: *common-service-options
    container_name: ${DOCKER_COMPOSE_DOMAIN_PREFIX}-unifi-network-app
    build:
      context: ../../../../docker-images/
      dockerfile: ./external/unifi-network-app/Dockerfile
      args:
        HOMELAB_BUILD_ENV: N/A
    depends_on:
      mongodb:
        condition: service_healthy
    environment:
      <<: *common-env-variables
      MONGO_DBNAME: unifi
      MONGO_HOST: mongodb
      MONGO_USER: unifi
    volumes:
      - ./app-data/unifi-network-app:/config:rw
      - ./app-secrets/mongodb-password.txt:/homelab/mongodb-password.txt:ro
    stop_grace_period: 60s

  mongodb:
    <<: *common-service-options
    container_name: ${DOCKER_COMPOSE_DOMAIN_PREFIX}-mongodb
    build:
      context: ../../../../docker-images/
      dockerfile: ./database/mongodb/Dockerfile
    environment:
      - DBNAME=unifi
      - USER=unifi
    volumes:
      - ./app-data/mongodb:/data/db:rw
      - ./app-secrets/mongodb-password.txt:/homelab/mongodb-password.txt:ro
      - ../../../../docker-images/database/mongodb/init-mongo-unifi-controller.js:/docker-entrypoint-initdb.d/init-mongo.js:ro
    stop_grace_period: 30s

  certificate-manager:
    <<: *common-service-options
    container_name: ${DOCKER_COMPOSE_DOMAIN_PREFIX}-certificate-manager
    build:
      context: ../../../../docker-images/
      dockerfile: ./custom/certificate-manager/Dockerfile
    env_file:
      - ./app-secrets/certificate-manager.env
    volumes:
      - ./app-data/certs:/homelab/certs:rw
      - ./app-logs/certificate-manager:/homelab/logs:rw

  http-proxy-admin:
    <<: *common-service-options
    container_name: ${DOCKER_COMPOSE_DOMAIN_PREFIX}-http-proxy-admin
    build:
      context: ../../../../docker-images/
      dockerfile: ./custom/http-proxy/Dockerfile
    depends_on:
      certificate-manager:
        condition: service_healthy
    environment:
      <<: *common-env-variables
      HOMELAB_CONTAINER_VARIANT: admin
    volumes:
      - ./app-data/certs:/homelab/certs:ro
      - ./app-secrets/http-user--proxy-status.htpasswd:/homelab/http-user--proxy-status.htpasswd:ro
      - ./app-logs/http-proxy-admin:/homelab/logs:rw

  http-proxy-portal:
    <<: *common-service-options
    container_name: ${DOCKER_COMPOSE_DOMAIN_PREFIX}-http-proxy-portal
    build:
      context: ../../../../docker-images/
      dockerfile: ./custom/http-proxy/Dockerfile
    depends_on:
      certificate-manager:
        condition: service_healthy
    environment:
      <<: *common-env-variables
      HOMELAB_CONTAINER_VARIANT: portal
    volumes:
      - ./app-data/certs:/homelab/certs:ro
      - ./app-secrets/http-user--proxy-status.htpasswd:/homelab/http-user--proxy-status.htpasswd:ro
      - ./app-logs/http-proxy-portal:/homelab/logs:rw

  socket-proxy:
    <<: *common-service-options
    container_name: ${DOCKER_COMPOSE_DOMAIN_PREFIX}-socket-proxy
    build:
      context: ../../../../docker-images/
      dockerfile: ./custom/socket-proxy/Dockerfile
    ports:
      - 1900:1900/udp
      - 3478:3478/udp
      - 5514:5514/udp
      - 6789:6789
      - 10001:10001/udp

  web-backup:
    <<: *common-service-options
    container_name: ${DOCKER_COMPOSE_DOMAIN_PREFIX}-web-backup
    build:
      context: ../../../../docker-images/
      dockerfile: ./custom/web-automation/Dockerfile
    depends_on:
      unifi-network-app:
        condition: service_healthy
    env_file:
      - ./app-secrets/web-backup.env
    environment:
      <<: *common-env-variables
      HOMELAB_CONTAINER_VARIANT: backup
    volumes:
      - ./app-data/web-backup:/homelab/backup:rw
      - ./app-logs/web-backup-errors:/homelab/errors:rw
      - ./app-logs/web-backup:/homelab/logs:rw

networks:
  internal-network:
    name: ${DOCKER_COMPOSE_DOMAIN_PREFIX}-internal-network
