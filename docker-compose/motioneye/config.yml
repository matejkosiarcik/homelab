apache:
  upstream-url:
    - path: /
      target: http://app:8765
    - path: /stream
      target: http://app:9081

prometheus:
  enabled: true
  endpoints:
    - path: /.apache/metrics
      auth:
        type: basic
