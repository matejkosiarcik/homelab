apache:
  upstream-url:
    - path: /
      target: http://app:8000

prometheus:
  enabled: true
  endpoints:
    - path: /projects/${<<APP>>__PROMETHEUS_PROJECT}/metrics
      auth:
        type: bearer
