x-definitions: {}

services:
  app-migrations:
    extends:
      file: ${DOCKER_COMPOSE_REPOROOT_PATH}/docker-compose/.shared/compose.yml
      service: app
    # Override:
    build:
      cache_from:
        - ${DOCKER_COMPOSE_BUILDCACHE_FROM}-app-migrations
      cache_to:
        - ${DOCKER_COMPOSE_BUILDCACHE_TO}-app-migrations
      dockerfile: ./docker-images/reportportal/migrations/Dockerfile
    container_name: ${DOCKER_COMPOSE_APP_SHORTNAME}-app-migrations
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - ${DOCKER_COMPOSE_APP_PATH}/app-secrets/app-migrations.env
    image: ${DOCKER_COMPOSE_APP_SHORTNAME}-app-migrations:1.0.0
    tmpfs:
      - /homelab/tmpfs:rw,noexec,nodev,noatime,nodiratime,uid=${DOCKER_COMPOSE_UID},gid=${DOCKER_COMPOSE_GID}

  app-index:
    extends:
      file: ${DOCKER_COMPOSE_REPOROOT_PATH}/docker-compose/.shared/compose.yml
      service: app
    # Override:
    build:
      cache_from:
        - ${DOCKER_COMPOSE_BUILDCACHE_FROM}-app-index
      cache_to:
        - ${DOCKER_COMPOSE_BUILDCACHE_TO}-app-index
      dockerfile: ./docker-images/reportportal/index/Dockerfile
    container_name: ${DOCKER_COMPOSE_APP_SHORTNAME}-app-index
    image: ${DOCKER_COMPOSE_APP_SHORTNAME}-app-index:1.0.0

  app-ui:
    extends:
      file: ${DOCKER_COMPOSE_REPOROOT_PATH}/docker-compose/.shared/compose.yml
      service: app
    # Override:
    build:
      cache_from:
        - ${DOCKER_COMPOSE_BUILDCACHE_FROM}-app-ui
      cache_to:
        - ${DOCKER_COMPOSE_BUILDCACHE_TO}-app-ui
      dockerfile: ./docker-images/reportportal/ui/Dockerfile
    container_name: ${DOCKER_COMPOSE_APP_SHORTNAME}-app-ui
    image: ${DOCKER_COMPOSE_APP_SHORTNAME}-app-ui:1.0.0
    tmpfs:
      - /etc/nginx/conf.d:rw,noexec,nodev,noatime,nodiratime,uid=${DOCKER_COMPOSE_UID},gid=${DOCKER_COMPOSE_GID}
      - /var/cache/nginx:rw,noexec,nodev,noatime,nodiratime,uid=${DOCKER_COMPOSE_UID},gid=${DOCKER_COMPOSE_GID}

  app-api:
    extends:
      file: ${DOCKER_COMPOSE_REPOROOT_PATH}/docker-compose/.shared/compose.yml
      service: app
    # Override:
    build:
      cache_from:
        - ${DOCKER_COMPOSE_BUILDCACHE_FROM}-app-api
      cache_to:
        - ${DOCKER_COMPOSE_BUILDCACHE_TO}-app-api
      dockerfile: ./docker-images/reportportal/api/Dockerfile
    container_name: ${DOCKER_COMPOSE_APP_SHORTNAME}-app-api
    depends_on:
      app-jobs:
        condition: service_healthy
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    env_file:
      - ${DOCKER_COMPOSE_APP_PATH}/app-secrets/app-api.env
    image: ${DOCKER_COMPOSE_APP_SHORTNAME}-app-api:1.0.0
    volumes:
      - ${DOCKER_COMPOSE_APP_PATH}/app-data/reportportal/data:/data/storage:rw
      - ${DOCKER_COMPOSE_APP_PATH}/app-logs/reportportal/api:/var/log/reportportal:rw

  app-uat:
    extends:
      file: ${DOCKER_COMPOSE_REPOROOT_PATH}/docker-compose/.shared/compose.yml
      service: app
    # Override:
    build:
      cache_from:
        - ${DOCKER_COMPOSE_BUILDCACHE_FROM}-app-uat
      cache_to:
        - ${DOCKER_COMPOSE_BUILDCACHE_TO}-app-uat
      dockerfile: ./docker-images/reportportal/uat/Dockerfile
    container_name: ${DOCKER_COMPOSE_APP_SHORTNAME}-app-uat
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - ${DOCKER_COMPOSE_APP_PATH}/app-secrets/app-uat.env
    image: ${DOCKER_COMPOSE_APP_SHORTNAME}-app-uat:1.0.0
    volumes:
      - ${DOCKER_COMPOSE_APP_PATH}/app-data/reportportal/data:/data/storage:rw

  app-jobs:
    extends:
      file: ${DOCKER_COMPOSE_REPOROOT_PATH}/docker-compose/.shared/compose.yml
      service: app
    # Override:
    build:
      cache_from:
        - ${DOCKER_COMPOSE_BUILDCACHE_FROM}-app-jobs
      cache_to:
        - ${DOCKER_COMPOSE_BUILDCACHE_TO}-app-jobs
      dockerfile: ./docker-images/reportportal/jobs/Dockerfile
    container_name: ${DOCKER_COMPOSE_APP_SHORTNAME}-app-jobs
    depends_on:
      postgres:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    env_file:
      - ${DOCKER_COMPOSE_APP_PATH}/app-secrets/app-jobs.env
    image: ${DOCKER_COMPOSE_APP_SHORTNAME}-app-jobs:1.0.0
    volumes:
      - ${DOCKER_COMPOSE_APP_PATH}/app-data/reportportal/data:/data/storage:rw

  app-analyzer:
    extends:
      file: ${DOCKER_COMPOSE_REPOROOT_PATH}/docker-compose/.shared/compose.yml
      service: app
    # Override:
    build:
      cache_from:
        - ${DOCKER_COMPOSE_BUILDCACHE_FROM}-app-analyzer
      cache_to:
        - ${DOCKER_COMPOSE_BUILDCACHE_TO}-app-analyzer
      dockerfile: ./docker-images/reportportal/analyzer/Dockerfile
    container_name: ${DOCKER_COMPOSE_APP_SHORTNAME}-app-analyzer
    depends_on:
      opensearch:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    env_file:
      - ${DOCKER_COMPOSE_APP_PATH}/app-secrets/app-analyzer.env
    image: ${DOCKER_COMPOSE_APP_SHORTNAME}-app-analyzer:1.0.0
    volumes:
      - ${DOCKER_COMPOSE_APP_PATH}/app-data/reportportal/data:/data/storage:rw

  postgres:
    extends:
      file: ${DOCKER_COMPOSE_REPOROOT_PATH}/docker-compose/.shared/compose.yml
      service: postgres

  rabbitmq:
    extends:
      file: ${DOCKER_COMPOSE_REPOROOT_PATH}/docker-compose/.shared/compose.yml
      service: template
    # Override:
    build:
      cache_from:
        - ${DOCKER_COMPOSE_BUILDCACHE_FROM}-rabbitmq
      cache_to:
        - ${DOCKER_COMPOSE_BUILDCACHE_TO}-rabbitmq
      dockerfile: ./docker-images/reportportal/rabbitmq/Dockerfile
    container_name: ${DOCKER_COMPOSE_APP_SHORTNAME}-rabbitmq
    env_file:
      - ${DOCKER_COMPOSE_APP_PATH}/app-secrets/rabbitmq.env
    image: ${DOCKER_COMPOSE_APP_SHORTNAME}-rabbitmq:1.0.0
    tmpfs:
      - /opt/bitnami/rabbitmq/etc/rabbitmq:rw,noexec,nodev,noatime,nodiratime,uid=${DOCKER_COMPOSE_UID},gid=${DOCKER_COMPOSE_GID}
      - /opt/bitnami/rabbitmq/var/lib/rabbitmq:rw,noexec,nodev,noatime,nodiratime,uid=${DOCKER_COMPOSE_UID},gid=${DOCKER_COMPOSE_GID}
      - /opt/bitnami/rabbitmq/.rabbitmq:rw,noexec,nodev,noatime,nodiratime,uid=${DOCKER_COMPOSE_UID},gid=${DOCKER_COMPOSE_GID}
      - /opt/bitnami/rabbitmq/.rabbitmq/mnesia:rw,noexec,nodev,noatime,nodiratime,uid=${DOCKER_COMPOSE_UID},gid=${DOCKER_COMPOSE_GID}

  opensearch:
    extends:
      file: ${DOCKER_COMPOSE_REPOROOT_PATH}/docker-compose/.shared/compose.yml
      service: template
    # Override:
    build:
      cache_from:
        - ${DOCKER_COMPOSE_BUILDCACHE_FROM}-opensearch
      cache_to:
        - ${DOCKER_COMPOSE_BUILDCACHE_TO}-opensearch
      dockerfile: ./docker-images/reportportal/opensearch/Dockerfile
    container_name: ${DOCKER_COMPOSE_APP_SHORTNAME}-opensearch
    image: ${DOCKER_COMPOSE_APP_SHORTNAME}-opensearch:1.0.0
    tmpfs:
      - /usr/share/opensearch/config:rw,noexec,nodev,noatime,nodiratime,uid=${DOCKER_COMPOSE_UID},gid=${DOCKER_COMPOSE_GID}
    ulimits:
      memlock:
        soft: -1
        hard: -1
    volumes:
      - ${DOCKER_COMPOSE_APP_PATH}/app-data/opensearch/data:/usr/share/opensearch/data:rw
      - ${DOCKER_COMPOSE_APP_PATH}/app-logs/opensearch/default:/usr/share/opensearch/logs:rw

  certificator:
    extends:
      file: ${DOCKER_COMPOSE_REPOROOT_PATH}/docker-compose/.shared/compose.yml
      service: certificator

  apache:
    extends:
      file: ${DOCKER_COMPOSE_REPOROOT_PATH}/docker-compose/.shared/compose.yml
      service: apache

  apache-prometheus-exporter:
    extends:
      file: ${DOCKER_COMPOSE_REPOROOT_PATH}/docker-compose/.shared/compose.yml
      service: apache-prometheus-exporter

  favicons:
    extends:
      file: ${DOCKER_COMPOSE_REPOROOT_PATH}/docker-compose/.shared/compose.yml
      service: favicons

networks:
  default:
    driver: none
  external-network:
    name: ${DOCKER_COMPOSE_APP_SHORTNAME}-external-network
    driver: bridge
    internal: false
  internal-network:
    name: ${DOCKER_COMPOSE_APP_SHORTNAME}-internal-network
    driver: bridge
    internal: true
