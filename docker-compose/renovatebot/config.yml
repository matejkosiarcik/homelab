app:
  name: Renovatebot

apache:
  upstream-url:
    - path: /
      target: http://app:8080

favicons:
  default: "@renovatebot.png"

gatus:
  enabled: true
  authentication:
    basic: homelab-viewer

healthchecks:
  - slug: certificator
  - slug: app
    schedule: "0 1,3,5 * * *"

prometheus:
  enabled: true
  endpoints:
    - path: /.apache/metrics
      auth:
        type: basic
