app:
  name: Git Cache

apache:
  upstream-url:
    - path: /
      target: http://app:8080

favicons:
  default: "@git-cache.png"

gatus:
  enabled: true

healthchecks:
  - slug: certificator

prometheus:
  enabled: true
  endpoints:
    - path: /.apache/metrics
      auth:
        type: basic
