app:
  name: DoneTick

apache:
  upstream-url:
    - path: /
      target: http://app:2021

gatus:
  enabled: true

healthchecks:
  - slug: certificator

prometheus:
  enabled: true
  endpoints:
    - path: /.apache/metrics
      auth:
        type: basic
