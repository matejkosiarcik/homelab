app:
  nice-name: Homepage

apache:
  upstream-url:
    - path: /
      target: http://app:3000

gatus:
  enabled: true
  authentication:
    basic: homelab-viewer

prometheus:
  enabled: true
  endpoints:
    - path: /.apache/metrics
      auth:
        type: basic
