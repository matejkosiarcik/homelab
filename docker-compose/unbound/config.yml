apache:
  upstream-url:
    - path: /metrics
      target: http://app-prometheus-exporter:9167/metrics
    - path: /
      target: http://file-server:8080

prometheus:
  enabled: true
  endpoints:
    - path: /.apache/metrics
      auth:
        type: basic
    - path: /metrics
      auth:
        type: basic
