app:
  nice-name: Gotify

apache:
  upstream-url:
    - path: /
      target: http://app:80

gatus:
  enabled: true

prometheus:
  enabled: true
  endpoints:
    - path: /.apache/metrics
      auth:
        type: basic
