# version: "3.8"

services:
  app:
    networks:
      unifi-controller-network:
        ipv4_address: 10.1.11.5
    restart: unless-stopped

  database:
    restart: unless-stopped

  webui-backup:
    restart: unless-stopped
