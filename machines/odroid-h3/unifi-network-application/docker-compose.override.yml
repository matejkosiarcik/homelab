# version: "3.8"

services:
  app:
    restart: "no"

  database:
    ports:
      - 27017:27017
    restart: "no"

  app-backup:
    volumes:
      - ../../../.shared/backup/entrypoint.sh:/app/entrypoint.sh:ro
      - ../../../.shared/backup/run.sh:/app/run.sh:ro
    restart: "no"
