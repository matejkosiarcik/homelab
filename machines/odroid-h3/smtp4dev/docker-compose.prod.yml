# version: "3.8"

services:
  smtp4dev:
    ports:
      - "25:25"
      - "80:80"
      # - "143:143"
    environment:
      - HOSTNAME=smtp4dev.lan
      - ServerOptions__HostName=smtp4dev.lan
    networks:
      pihole-network:
        ipv4_address: 10.1.11.2
