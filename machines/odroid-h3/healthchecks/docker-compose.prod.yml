# version: "3.8"

services:
  app:
    ports:
      - "80:8000"
    environment:
      - ALLOWED_HOSTS=healthchecks.lan
      - DEFAULT_FROM_EMAIL=healthchecks@healthchecks.lan
      - EMAIL_HOST=smtp4dev.lan
      - REGISTRATION_OPEN=False
      - SITE_ROOT=http://healthchecks.lan
