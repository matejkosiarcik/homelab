# version: "3.8"

services:
  app:
    # TODO: Add Healthcheck with wget
    image: b4bz/homer:v24.04.1
    container_name: homer
    hostname: homer
    environment:
      - INIT_ASSETS=0
      - PORT=80
    ports:
      - "8080:80"
    volumes:
      - ./config/assets/:/www/assets:ro

networks:
  homer-network:
    driver: macvlan
    driver_opts:
      parent: eth0
    ipam:
      config:
        - subnet: "10.1.0.0/19"
          ip_range: "10.1.11.0/24"
