# version: "3.8"

services:
  app:
    ports:
      - 80:3001
    networks:
      uptime-kuma-network:
        ipv4_address: 10.1.11.6
    restart: unless-stopped

  app-backup:
    restart: unless-stopped
