# version: "3.8"

services:
  uptime-kuma-app:
    ports:
      - 80:3001
    networks:
      uptime-kuma-network:
        ipv4_address: 10.1.11.6
    restart: unless-stopped

  uptime-kuma-app-backup:
    volumes:
      - ../.shared/ui-backup/entrypoint.sh:/app/entrypoint.sh:ro
      - ../.shared/ui-backup/run.sh:/app/run.sh:ro
    restart: unless-stopped
