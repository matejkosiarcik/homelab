version: "3.8"

services:
  app:
    image: httpd:2.4.59
    container_name: apache
    restart: unless-stopped
    volumes:
      - "./html:/usr/local/apache2/htdocs"

networks:
  # debug:
  #   driver: macvlan
  #   driver_opts:
  #     parent: eth0
  #   ipam:
  #     config:
  #       - subnet: "10.1.0.0/19"
  #         ip_range: "10.1.6.0/24"
  #         gateway: "10.1.0.1"
  debug2:
    driver: ipvlan
    driver_opts:
      parent: eth0
      ipvlan_mode: l2
    ipam:
      config:
        - subnet: "10.1.0.0/19"
          ip_range: "10.1.6.0/24"
          gateway: "10.1.0.1"
